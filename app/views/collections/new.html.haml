%h1=  @project.name + (@collection.is_root? ? " >> New" : "  >>  " + get_collection_path(@collection.parent) + " >> New") 

= render 'form'

#spacer=  "==============================================="

= link_to @project.name, @collection.is_root? ? parent_url : resource_path(@collection.parent)
